#include <vector>
using namespace std;

vector<long long> taglia(int N, int M, int Q, vector<vector<long long>> A, vector<long long> K) {
    vector<long long> R(Q, 42);
    for (int i = 0; i < Q; i++) R[i] = 42;
    return R;
}
